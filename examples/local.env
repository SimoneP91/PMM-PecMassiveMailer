# The local stack of docker-compose.yml, reached from this machine.
# Used by `npm run local:publish` and `npm run local:outcomes`; no secret here.
RABBITMQ_URL=amqp://pecmailer:pecmailer@localhost:5672
PECMAILER_TENANT=serfin
PECMAILER_MAILBOX=serfin-aruba
